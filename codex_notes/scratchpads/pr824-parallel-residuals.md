# Experiment Note: pr824-parallel-residuals

Prepared branch:

- `/Users/wulfie/code/parameter-golf-worktrees/pr824-parallel-residuals`

Branch-local implementation and notes live in that worktree. The current main-branch summary is:

- exploit stack combining PR824 mimic with a minimal late-layer ParallelResiduals path
- syntax-checked only, no local MLX run yet
